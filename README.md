# Portfolio — Trần Lê Trung Kiên

Trang portfolio cá nhân (.NET Backend · DevOps / GitOps).

- **Live:** https://tranletrungkien1705.github.io
- Static HTML tự chứa (CSS/JS inline, không phụ thuộc CDN) — nhẹ, tải nhanh, offline được.
- **CI/CD:** mỗi `git push` lên `main` → GitHub Actions tự build + deploy lên GitHub Pages
  (xem [.github/workflows/deploy.yml](.github/workflows/deploy.yml)).

## Sửa nội dung
Toàn bộ nằm trong [index.html](index.html). Đổi text/link rồi `git push` là site tự cập nhật.
